@model IEnumerable<Infraestructure.Models.plan_cobro>

@{
    ViewBag.Title = "Lista Plan Cobro";
}

<h2>Lista de Planes de Cobro</h2>

<p>
    @Html.ActionLink("Crear nuevo", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.detail)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.residenciaId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.rubroCobroId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.datePlan)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.estado)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.total)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.detail)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.residenciaId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.rubro_cobro.detalle)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.datePlan)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.estado)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.total)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
